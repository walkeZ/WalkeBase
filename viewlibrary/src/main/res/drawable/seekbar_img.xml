<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!--3个层的顺序即为显示时的叠加顺序-->
    <!--背景色-->
    <item android:id="@android:id/background" android:drawable="@drawable/seek_bar_blue0">
        <shape>
            <corners android:radius="5dip" />

            <!--<solid android:color="#CCCCCC" />-->

        </shape>
    </item>

    <!--二级进度条的颜色-->
    <item android:id="@android:id/secondaryProgress" android:drawable="@drawable/seek_bar_red0">
        <clip>
            <shape>
                <corners android:radius="5dip" />

                <!--<solid android:color="#88F56100" />-->
            </shape>
        </clip>
    </item>

    <!--一级进度条的颜色，也可以直接替换成图片-->
    <item android:id="@android:id/progress" android:drawable="@drawable/seek_bar_red0">
        <clip>
            <shape>
                <corners android:radius="5dip" />

                <!--<solid android:color="#F56100" />-->
            </shape>
        </clip>
    </item>

</layer-list>
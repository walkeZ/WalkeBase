<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="walke.viewlibrary">

    <application android:allowBackup="true"
                 android:label="@string/app_name"
                 android:supportsRtl="true">
        <activity android:name=".ViewActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".sunset.SunAnimationActivity"/>
        <activity android:name=".seekbar.SeekBarActivity"/>
        <activity android:name=".marquee.MarqueeActivity1"/>
        <activity android:name=".flowlayout.FlowLayoutActivity"/>
        <activity android:name=".percentage.PercentageJavaActivity"/>
        <activity android:name=".luckypan.LuckyPanActivity"/>

    </application>

</manifest>
